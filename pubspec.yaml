name: flutter_polyline_decorator
description: Direction decorator to polyline.
version: 0.0.3
homepage: https://github.com/nickBrabers/flutter_polyline_decorator

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^4.0.0
  latlong2: ^0.8.2
  maps_toolkit: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: